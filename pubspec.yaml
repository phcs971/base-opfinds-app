name: # TODO Nome do Pacote
description: # TODO Descricao do Pacote

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  equatable: ^1.2.3
  bloc: ^6.0.1
  flutter_bloc: ^6.0.1
  get_it: ^4.0.4
  logger: ^0.9.2
  flutter_icons: ^1.1.0
  device_preview: ^0.4.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.7.5

#flutter pub run flutter_launcher_icons:main
flutter_icons:
  ios: true
  android: true
  # image_path: "assets/images/icon.png"

flutter:
  uses-material-design: true
  # assets:
    # - assets/images/icon.png
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: assets/fonts/Schyler-Regular.ttf
  #       - asset: assets/fonts/Schyler-BoldItalic.ttf
  #         style: italic
  #         weight: 700

